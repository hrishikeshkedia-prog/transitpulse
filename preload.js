// Exposes only what the renderer needs from Node — nothing here yet.
// localStorage works natively inside Electron's renderer, so no bridge needed.
window.addEventListener('DOMContentLoaded', () => {});
